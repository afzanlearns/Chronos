services:
  chronos-tracker:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - chronos_data:/root/.chronos
    environment:
      - CHRONOS_ENV=production
      - CHRONOS_DB_PATH=/root/.chronos/chronos.db
      - FLASK_PORT=5000

volumes:
  chronos_data:
